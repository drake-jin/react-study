/* eslint no-undef: "off" */

const { assert } = require('chai')

describe('Configure Check', () => {
  it('MySQL Configuration is validated', (done) => {
    done()
  })

  it('NaverCafeCrawler is validated', (done) => {
    done()
  })
})
